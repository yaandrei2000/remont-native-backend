# Backend API для ФастСервис

Backend приложение на NestJS с использованием Prisma ORM и PostgreSQL для платформы быстрого поиска и заказа услуг по ремонту техники.

## 🚀 Технологии

- **NestJS** - прогрессивный Node.js фреймворк для построения эффективных и масштабируемых серверных приложений
- **Prisma** - современный ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация и авторизация пользователей
- **TypeScript** - типизированный JavaScript для надежности кода
- **Passport** - стратегии аутентификации
- **Expo Push Notifications** - отправка push-уведомлений мастерам

## 📋 Требования

- Node.js >= 18.x
- PostgreSQL >= 14.x
- npm или yarn

## 🔧 Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте базу данных:
   - Создайте PostgreSQL базу данных
   - Обновите `DATABASE_URL` в файле `.env` (см. раздел "Переменные окружения")

3. Запустите миграции:
```bash
npm run prisma:migrate
```

4. Сгенерируйте Prisma Client:
```bash
npm run prisma:generate
```

5. Заполните базу тестовыми данными (опционально):
```bash
npm run prisma:seed
```

## 🏃 Запуск

### Режим разработки
```bash
npm run start:dev
```
Сервер запустится на `http://localhost:3000` с hot-reload.

### Продакшен
```bash
npm run build
npm run start:prod
```

### Отладка
```bash
npm run start:debug
```

## 📡 API Endpoints

### Аутентификация

- `POST /auth/send-code` - Отправка SMS кода подтверждения на телефон
- `POST /auth/verify-code` - Проверка кода и получение JWT токенов
- `POST /auth/refresh` - Обновление access token

### Пользователи

- `GET /users/me` - Получение профиля текущего пользователя (требует авторизации)
- `PATCH /users/me` - Обновление профиля (имя, фамилия, email)
- `PATCH /users/me/city` - Обновление города пользователя
- `PATCH /users/me/push-token` - Сохранение push-токена для уведомлений

### Города

- `GET /cities` - Получение списка всех городов

### Услуги

- `GET /services/categories` - Получение всех категорий услуг
- `GET /services/categories/:slug` - Получение услуг по категории
- `GET /services/search?q=query` - Поиск услуг по названию
- `GET /services/categories/:categorySlug/services/:serviceSlug` - Получение конкретной услуги

### Заказы

#### Для клиентов:
- `POST /orders` - Создание нового заказа (требует авторизации)
- `GET /orders` - Получение списка заказов пользователя (требует авторизации)
- `GET /orders/:id` - Получение детальной информации о заказе (требует авторизации)
- `POST /orders/:id/cancel` - Отмена заказа клиентом (только PENDING/CONFIRMED, без мастера)

#### Для мастеров:
- `GET /orders/available` - Получение доступных заказов в городе мастера
- `GET /orders/master` - Получение заказов мастера
- `GET /orders/:id/master` - Получение детальной информации о заказе для мастера
- `POST /orders/:id/assign` - Взятие заказа мастером
- `PATCH /orders/:id/status` - Обновление статуса заказа
- `PATCH /orders/:id/steps/:stepId` - Обновление этапа выполнения заказа

### Админ-панель

Все админские endpoints требуют роль `ADMIN` и авторизацию.

#### Пользователи:
- `GET /admin/users` - Список пользователей с пагинацией и поиском
- `GET /admin/users/:id` - Детали пользователя
- `PATCH /admin/users/:id` - Обновление пользователя
- `DELETE /admin/users/:id` - Удаление пользователя

#### Заказы:
- `GET /admin/orders` - Список всех заказов с фильтрацией
- `GET /admin/orders/:id` - Детали заказа
- `PATCH /admin/orders/:id` - Обновление заказа

#### Мастера:
- `GET /admin/masters` - Список мастеров с фильтрацией
- `POST /admin/masters/:id/approve` - Одобрение мастера
- `POST /admin/masters/:id/block` - Блокировка мастера

#### Категории:
- `GET /admin/categories` - Список категорий
- `POST /admin/categories` - Создание категории
- `PATCH /admin/categories/:id` - Обновление категории
- `DELETE /admin/categories/:id` - Удаление категории

#### Услуги:
- `GET /admin/services` - Список услуг
- `POST /admin/services` - Создание услуги
- `PATCH /admin/services/:id` - Обновление услуги
- `DELETE /admin/services/:id` - Удаление услуги

#### Статистика:
- `GET /admin/statistics` - Общая статистика платформы

## 🔐 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/remont_db?schema=public"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
JWT_REFRESH_EXPIRES_IN=7d

# Сервер
PORT=3000
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:8081
```

## 📊 Структура базы данных

### Основные модели:

- **User** - пользователи (клиенты, мастера, админы)
  - Роли: `CLIENT`, `MASTER`, `ADMIN`
  - Поля для мастеров: `rating`, `reviewsCount`, `isActive`, `cityId`
  - Push-токен для уведомлений

- **City** - города обслуживания

- **AuthCode** - коды подтверждения для входа по SMS

- **ServiceCategory** - категории услуг (например, "Ремонт телефонов")

- **Service** - услуги (например, "Замена экрана iPhone")

- **Order** - заказы
  - Статусы: `PENDING`, `CONFIRMED`, `ASSIGNED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`
  - Связи с клиентом и мастером

- **OrderItem** - позиции заказа (услуги в заказе)

- **OrderStep** - этапы выполнения заказа
  - Статусы: `PENDING`, `COMPLETED`
  - Автоматическое обновление при назначении мастера

- **PromoCode** - промокоды для скидок

- **Referral** - реферальная программа

## 🛠 Разработка

### Prisma Studio

Для просмотра и редактирования данных в базе:
```bash
npm run prisma:studio
```

### Миграции

Создание новой миграции:
```bash
npm run prisma:migrate
```

Применение миграций без создания новой:
```bash
npm run db:push
```

### Форматирование кода

```bash
npm run format
```

### Линтинг

```bash
npm run lint
```

## 🧪 Тестирование

```bash
# unit tests
npm run test

# e2e tests
npm run test:e2e

# test coverage
npm run test:cov

# watch mode
npm run test:watch
```

## 📁 Структура проекта

```
backend/
├── prisma/
│   ├── schema.prisma      # Схема базы данных
│   ├── seed.ts            # Сидер для тестовых данных
│   └── migrations/        # Миграции базы данных
├── src/
│   ├── admin/             # Админ-панель
│   │   ├── admin.controller.ts
│   │   ├── admin.service.ts
│   │   └── dto/           # DTO для админских операций
│   ├── auth/              # Аутентификация
│   │   ├── auth.controller.ts
│   │   ├── auth.service.ts
│   │   ├── jwt.strategy.ts
│   │   ├── jwt-auth.guard.ts
│   │   └── admin.guard.ts
│   ├── cities/            # Города
│   ├── common/            # Общие утилиты
│   │   ├── decorators/    # Кастомные декораторы
│   │   └── dto/           # Общие DTO
│   ├── notifications/     # Push-уведомления
│   ├── orders/            # Заказы
│   │   └── dto/
│   ├── prisma/            # Prisma сервис
│   ├── services/          # Услуги
│   ├── users/             # Пользователи
│   │   └── dto/
│   ├── app.module.ts      # Главный модуль
│   └── main.ts            # Точка входа
├── test/                  # E2E тесты
└── package.json
```

## 🔔 Push-уведомления

Система автоматически отправляет push-уведомления мастерам при создании нового заказа в их городе. Для работы требуется:

1. Сохранение push-токена через `PATCH /users/me/push-token`
2. Настроенный Expo Push API (используется Expo Push Notification Service)

## 🔒 Безопасность

- JWT токены для аутентификации
- Refresh tokens для обновления сессий
- Role-based access control (RBAC)
- Валидация данных через class-validator
- Защита от SQL-инъекций через Prisma

## 📝 Лицензия

UNLICENSED
